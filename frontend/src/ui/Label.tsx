// Use.
import styled from 'styled-components';

/**
 * Provide ui component Label.
 */
const StyledLabel = styled.label`
  display: flex;
  flex-direction: column;
  margin-bottom: 30px;
`;

export { StyledLabel };
