// Use.
import { Text, Container } from '../ui';

/**
 * Provide screen Homepage.
 */
const Homepage = () => {
  return (
    <Container spacing={30} direction="horizontal" center={true}>
      <Text variant="h2">Reporting</Text>
    </Container>
  );
};

export { Homepage };
